---
const SCRIPT_PATH = "/smart-video-app/smart-video-app.js";
const INSTALL_PATH = "/smart-video-app/install";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Smart Video App</title>
    <style>
      :root {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        background-color: #f8fafc;
        color: #0f172a;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      main {
        max-width: 42rem;
        background: #fff;
        border-radius: 1.25rem;
        padding: 2.5rem;
        box-shadow: 0 20px 40px rgba(15, 23, 42, 0.12);
      }

      h1 {
        margin-top: 0;
        font-size: 2.25rem;
      }

      p {
        color: #475569;
        line-height: 1.6;
      }

      a {
        color: #2563eb;
        text-decoration: none;
        font-weight: 600;
      }

      ul {
        padding-left: 1.5rem;
        color: #475569;
      }
    </style>
  </head>
  <body>
    <main>
      <h1>Smart Video App</h1>
      <p>
        Smart Video speeds up Webflow sites by lazy-loading YouTube and Vimeo embeds only when visitors
        scroll near them. It replaces heavy players with lightweight placeholders, keeping your media
        pages fast and responsive without breaking playback.
      </p>
      <ul>
        <li>Auto-detects Webflow video components and custom iframe embeds.</li>
        <li>Renders placeholders instantly with custom thumbnails and play buttons.</li>
        <li>Works with Webflow Cloud, static publishing, and custom domains.</li>
      </ul>
      <p>
        <a href={SCRIPT_PATH}>View the hosted Smart Video script</a><br />
        <a href={INSTALL_PATH}>Go to the install page</a>
      </p>
    </main>
  </body>
</html>
